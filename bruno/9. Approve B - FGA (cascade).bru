meta {
  name: 9. Approve B - FGA (cascade)
  type: http
  seq: 9
}

post {
  url: {{base_url}}/fga/payouts/D2/approve
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  base_url: http://localhost:8080
}

assert {
  res.status: eq 403
}

docs {
  Disbursement B also denied — from the SAME single tuple deletion at the team edge.
  Kwame never had a direct tuple on B; it resolved via can_approve from parent.
  Expected: 403 — the cascade.
}
