meta {
  name: 8. Approve A - FGA (fix)
  type: http
  seq: 8
}

post {
  url: {{base_url}}/fga/payouts/D1/approve
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  base_url: http://localhost:8080
}

assert {
  res.status: eq 403
}

docs {
  Same token, after fire. FGA re-derives authz live from the graph.
  Expected: 403 — THE FIX. Reality changed, the system knows.
}
